"use server"
import React from 'react';

const emailTemplate = async (data) => {
    console.log(data);
    return (
        <div>
             
        </div>
    );
};

export default emailTemplate;